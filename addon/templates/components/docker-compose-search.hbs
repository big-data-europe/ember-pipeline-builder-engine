{{#power-select
  allowClear=false
  options=dockerComposeList.lastSuccessful.value
  search=(perform dockerComposeList)
  selected=model.dockerFile
  onchange=(action "saveNewDockerFile")
  as |option|
}}
  {{option.title}}
{{/power-select}}
